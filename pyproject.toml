[project]
name = "{{project_name}}"
version = "0.1.0"
description = "{{project_name}} project"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
